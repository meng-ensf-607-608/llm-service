input_variables=['age', 'chronic_conditions', 'complaints', 'gender', 'occupation'] 
input_types={} 
partial_variables=
{
    'format_instructions': 
    'The output should be formatted as a JSON instance that conforms to the JSON schema below.
    \n\n
    As an example, for the schema 
    {
        "properties": {
            "foo": {
                "title": "Foo", "description": "a list of strings", 
                "items": {"type": "string"},
                "type": "array"
            }
        }, 
        "required": ["foo"]
    }
    \n
    the object {"foo": ["bar", "baz"]} is a well-formatted instance of the schema. 
    The object {"properties": {"foo": ["bar", "baz"]}} is not well-formatted.
    \n\n
    Here is the output schema:\n
    ```
    \n
    {
        "$defs": {
            "Cause": {
                "properties": {
                    "cause": {"title": "Cause", "type": "string"}, 
                    "severity": {"exclusiveMinimum": 0, "title": "Severity", "type": "integer"}, 
                    "symptoms": {"items": {"type": "string"}, 
                    "title": "Symptoms", "type": "array"}
                }, 
                "required": ["cause", "severity", "symptoms"], 
                "title": "Cause", 
                "type": "object"
            }
        }, 
        "properties": {
            "likely_causes": {
                "items": {"$ref": "#/$defs/Cause"},
                "title": "Likely Causes", 
                "type": "array"
            }, 
            "lifestyle_changes": {
                "items": {"type": "string"}, 
                "title": "Lifestyle Changes", 
                "type": "array"
            }, 
            "dietary_changes": {
                "items": {"type": "string"}, 
                "title": "Dietary Changes", 
                "type": "array"
            }
        }, 
        "required": ["likely_causes", "lifestyle_changes", "dietary_changes"]
    }
    \n
    ```'
} 
template=
"
\n
You are an intelligent family physician assistant.
\n\n\n
You have a patient complaining about: {complaints}
\n
Patient Details:\nAge: {age}\nGender: {gender}\nOccupation: {occupation}\nKnown chronic conditions: {chronic_conditions}
\n\n
Considering the patient's gender, age, and occupation, list the most likely causes. 
\n
Rank them based on increasing severity; 1 for the cause with low severity. 
\n
Include the severity and list of symptoms in the response.
\n
Suggest possible lifestyle changes and dietary changes to remedy the condition.
\n
Generate a complete JSON object with likely causes, lifestyle changes, and dietary changes based on the provided symptoms.
\n
{format_instructions}
\n
"